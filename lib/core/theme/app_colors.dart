import 'package:flutter/material.dart';

class AppColors {
  // Ganti '#' menjadi '0xFF'
  static const Color primary = Color(0xFFD9381E);    // Tadi: 0xD9381E
  static const Color secondary = Color(0xFFFFFFFF);  // Tadi: 0xFFFFFF (putih)
  static const Color tertiary = Color(0xFF0078CB);   // Tadi: 0x0078CB
  static const Color neutral = Color(0xFF89726D);    // Tadi: 0x89726D
  
  static const Color background = Color(0xFFF8F9FA); // Ini udah bener
  static const Color textPrimary = Color(0xFF333333); // Ini udah bener
  static const Color textSecondary = Color(0xFF888888); // Ini udah bener
}